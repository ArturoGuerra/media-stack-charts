{{- if .Values.node.enabled }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ template "configmap.fullname" . }}-node
  labels: {{ include "labels" . | nindent 4 }}
data: {{ toYaml .Values.node.env | nindent 2 }}
{{- end }}
